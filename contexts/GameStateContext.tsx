import { GameStateProvider, useGameState } from '../hooks/useGameState';

// This file now re-exports the context and provider from the hooks directory
// to maintain the original import structure throughout the app.

export { GameStateProvider, useGameState };
